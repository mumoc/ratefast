= link_to 'Create new Voting', new_admin_voting_path

%table
  %thead
    %tr
      %th ID
      %th Title
      %th Status
      %th Actions
  %tbody
    - @votings.each do |voting|
      %tr
        %td= voting.id
        %td= voting.title
        %td= voting.status
        %td
          = link_to 'Edit', edit_admin_voting_path(voting)
          = link_to 'Delete', admin_voting_path(voting), method: :delete
